PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/highway10
# Optional email — forms still save to DB / memory without these
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
NOTIFY_EMAIL=
CLIENT_ORIGIN=http://localhost:5173
